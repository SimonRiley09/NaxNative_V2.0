version: '3'
services:
  database:
    build: ./database
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: api_keys
    networks:
      - backend  
    
  data-server:
    build: ./backend-data
    depends_on:
      - database
    ports:
      - "1025:1025"
    networks:
      - backend
  
  data-auth:
    build: ./backend-auth
    depends_on:
      - database
    ports:
      - "1028:1028"
    networks:
      - backend
networks:
  backend: